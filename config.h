/* config.h — sfx user configuration */

#define EDITOR "vic"       /* text editor for opening files */
#define SHELL "bash"       /* shell for :sh and running commands */
#define RCFILE "~/.bashrc" /* sourced before every status-bar command */
